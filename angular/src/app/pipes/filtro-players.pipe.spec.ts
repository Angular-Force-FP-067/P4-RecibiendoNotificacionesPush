import { FiltroPlayersPipe } from './filtro-players.pipe';

describe('FiltroPlayersPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPlayersPipe();
    expect(pipe).toBeTruthy();
  });
});
